# Agent instructions

See [CLAUDE.md](CLAUDE.md). The guidance there applies to every AI coding agent working in this repo, not only Claude. It's kept in one file rather than copied across several so the copies can't drift apart.
